# AXIOM v0.8.0 RC Smoke

## Local verification

```bash
npm install
npm test
node --test lib/atp-conformance.test.js
node --test lib/axiom-package-format.test.js
node --test packages/axiom-verify/index.test.js
```

Expected:

- `npm test` passes `486/486`
- `lib/atp-conformance.test.js` passes
- `lib/axiom-package-format.test.js` passes
- `packages/axiom-verify/index.test.js` passes

## HTTP smoke

If the server is supported:

```bash
node server.js
```

In another terminal:

```bash
curl.exe http://localhost:3000/
curl.exe http://localhost:3000/health
curl.exe http://localhost:3000/v2-status
curl.exe http://localhost:3000/graph-data
curl.exe http://localhost:3000/api/provenance
curl.exe http://localhost:3000/api/audit
curl.exe http://localhost:3000/api/candidate-claims
curl.exe http://localhost:3000/api/trust-receipt
```

Expected:

- `/v2-status` returns `version=0.8.0`
- `/v2-status` returns `testStatus=486/486`
- existing HTTP smoke endpoints still return `200`
- read-only trust endpoints do not mutate state

## Clean clone validation

```bash
cd <clean-test-dir>
git clone <repo-url> axiom-v08-rc-test
cd axiom-v08-rc-test
git checkout v0.8-trust-kernel
npm install
npm test
node --test packages/axiom-verify/index.test.js
```

If HTTP smoke is supported:

```bash
node server.js
```

Then repeat the HTTP smoke commands above.
